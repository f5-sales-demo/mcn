---
title: การเข้าถึง root เสริม
description: >-
  สองคำสั่งที่สร้างและลบบัญชี root ชื่อ xuser บน Customer Edge
  ใช้ตามคำแนะนำของฝ่ายสนับสนุน คำสั่งเหล่านี้ทำให้โหนดมีการเปลี่ยนแปลง
  และคำสั่งสร้างเป็นวิธีเดียวที่มีเอกสารระบุในการเข้าถึงไฟล์ที่บัญชี admin
  ไม่สามารถอ่านได้
sidebar:
  order: 70
  label: การเข้าถึง Root
i18n:
  sourceHash: c2417b409442
  translator: machine
---

import { Aside } from "@astrojs/starlight/components";

สองคำสั่งสร้างและลบบัญชี root ที่มีชื่อว่า `xuser` คำสั่งเหล่านี้เป็นของ F5 ไม่ปรากฏใน
[debug API](../../../access/debug-api/) และเป็นช่องทางหลีกเลี่ยงสำหรับงานที่บัญชี `admin`
ไม่สามารถดำเนินการได้

<Aside type="danger" title="คำสั่งเหล่านี้เปลี่ยนแปลงสถานะบัญชีของโหนด และไม่ได้ถูกรันที่นี่">
`get-auxilary-root-access-to-node` สร้าง **root user** ซึ่งเป็นการเปลี่ยนแปลงโหนด
ที่คงอยู่หลังจากเซสชันของคุณสิ้นสุด และไม่ใช่สิ่งที่ควรทำในลักษณะสำรวจ คำสั่งทั้งสองมีเอกสารระบุจากเมนูความสมบูรณ์ของอุปกรณ์เอง ไม่มีการรันคำสั่งใด และ harvest harness ปฏิเสธทั้งสองคำสั่ง

การสะกดผิดของ "auxiliary" เป็นของอุปกรณ์ ไม่ใช่การพิมพ์ผิดในหน้านี้ พิมพ์ตามที่แสดงไว้ มิฉะนั้นจะไม่พบคำสั่ง
</Aside>

## `get-auxilary-root-access-to-node`

สร้างบัญชี root ชื่อ `xuser`

```bash
execcli get-auxilary-root-access-to-node
```

**เมื่อนี่คือคำตอบที่แท้จริง** การวินิจฉัยบางอย่างไม่สามารถเข้าถึงได้ในฐานะ `admin`
ตัวอย่างที่ชัดเจนที่สุดที่ตรวจสอบแล้วบน build นี้คือ [`kubelet-get-params`](../kubelet/) ซึ่ง
ล้มเหลวพร้อมข้อความ `open /etc/vpm/kubelet-custom-params.txt: permission denied` — ไฟล์มีอยู่แต่
บัญชี `admin` ไม่สามารถอ่านได้ การเข้าถึง root เป็นเส้นทางที่มีเอกสารระบุสำหรับปัญหาประเภทนี้

**ก่อนที่จะใช้คำสั่งนี้** ให้พิจารณาว่า [debug API](../../../access/debug-api/)
ตอบคำถามได้แล้วหรือไม่ ระดับ `exec` ของมันรันคำสั่งที่มีสิทธิ์สูงโดยไม่สร้างบัญชีถาวร
ซึ่งเป็นการเปลี่ยนแปลงโหนดที่น้อยกว่า

## `remove-auxilary-root-access-to-node`

ลบบัญชี `xuser`

```bash
execcli remove-auxilary-root-access-to-node
```

**รันคำสั่งนี้เมื่อคุณดำเนินการเสร็จสิ้น** บัญชี root ที่สร้างขึ้นสำหรับเซสชันวินิจฉัยหนึ่งและถูกทิ้งไว้
เป็น credential ถาวรบนอุปกรณ์ที่ไม่มีสิ่งใดจะเตือนคุณเกี่ยวกับมัน ให้ถือว่าการสร้างและการลบเป็นคู่กัน
ในเซสชันเดียวกัน เช่นเดียวกับที่
[`vifdump`](../packet-capture/) และ `vifdump-file-rm` เป็นคู่กัน

<Aside type="note" title="สิ่งที่ยังไม่ได้รับการยืนยัน">
ไม่มีการบันทึก output ของคำสั่งใด ดังนั้นหน้านี้ไม่สามารถบอกคุณได้ว่าการดำเนินการสำเร็จมีหน้าตาอย่างไร
รหัสผ่านหรือ key ที่ `xuser` ถูกสร้างขึ้นด้วย หรือการลบถูกปฏิเสธหรือไม่ในขณะที่เซสชันเปิดอยู่
สิ่งเหล่านี้ต้องการโหนดที่คุณยินดีจะทำการแก้ไข
</Aside>
