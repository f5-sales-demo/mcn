---
title: curl
description: >-
  การทดสอบการเข้าถึงจากบริบทเครือข่ายสองแบบที่แตกต่างกันบนโหนด
  การเลือกใช้แบบใดเป็นตัวกำหนดว่าความล้มเหลวนั้นพิสูจน์อะไรได้จริง
sidebar:
  order: 80
  label: curl
i18n:
  sourceHash: 6525a6aa3a90
  translator: machine
---

import { Aside } from "@astrojs/starlight/components";

สองคำสั่ง และความแตกต่างระหว่างทั้งสองคือคุณค่าทั้งหมด

## `curl-host`

ทำงานใน host network namespace — การเชื่อมต่อของโหนดเอง ก่อนที่ดาต้าเพลนจะเข้ามาเกี่ยวข้อง

```bash
{"command":["curl-host","-v","cloud.f5.com"]}
```

```text file=../../../../_data/sitecli-curl-host.txt
```

## `curl-vega`

ทำงานในบริบท Vega (คอนโทรลเพลน) แทนที่จะเป็นของโฮสต์ ความแตกต่างที่สังเกตได้
และเป็นสิ่งเดียวที่ยืนยันได้ในที่นี้ คือสภาพแวดล้อมที่มันสืบทอดมา: `curl-vega` รายงานรายการ
`NO_PROXY` ที่ครอบคลุมช่วงที่อยู่ส่วนตัว, `int.ves.io`, `int.volterra.us` และโดเมน
`.tenant.local` ของไซต์เอง ส่วน `curl-host` ไม่รายงานเลย สิ่งที่นัยยะนี้บ่งบอกเกี่ยวกับเส้นทาง DNS
นั้นไม่ได้ถูกวัดไว้

```bash
{"command":["curl-vega","-v","cloud.f5.com"]}
```

```text file=../../../../_data/sitecli-curl-vega.txt
```

<Aside type="tip" title="คู่คำสั่งนี้คือการแบ่งครึ่งเพื่อค้นหาปัญหา">
รันทั้งสองคำสั่ง หาก `curl-host` สำเร็จแต่ `curl-vega` ล้มเหลว แสดงว่าโหนดมีการเชื่อมต่อ แต่
บางอย่างในบริบทของคอนโทรลเพลนไม่มี — อาจเป็นการตั้งค่าพร็อกซีหรือรีโซลเวอร์ของมัน หากทั้งสอง
ล้มเหลว ปัญหาอยู่เหนือโหนดขึ้นไป: การกำหนดเส้นทาง ไฟร์วอลล์ หรือเส้นทางขาออก หากมีเพียง
`curl-host` ที่ล้มเหลว แสดงว่าคุณพบสิ่งผิดปกติ และควรตรวจสอบตารางเส้นทางของโฮสต์ก่อน

บนโหนดที่มีสุขภาพดี ทั้งสองคำสั่งจะสำเร็จและเข้าถึงที่อยู่เดียวกัน ดังนั้นผลลัพธ์ที่สะอาดทั้งคู่จึงไม่ได้พิสูจน์อะไร
เกี่ยวกับว่ากำลังใช้บริบทใดอยู่ — *ความแตกต่าง* ต่างหากที่ให้ข้อมูล
</Aside>

ทั้งสองคำสั่งรับอาร์กิวเมนต์ `curl` ปกติในรูปแบบสมาชิกอาร์เรย์แยกกัน `-v` ควรค่าแก่การคงไว้
เพราะบรรทัดของการเชื่อมต่อและการทำ TLS handshake มักเป็นจุดที่ความล้มเหลวปรากฏให้เห็น
