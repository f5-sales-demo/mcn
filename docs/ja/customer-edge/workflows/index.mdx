---
title: 診断ワークフロー
description: >-
  コマンドではなく症状が先にある場合は、この階層をお読みください。各ワークフローは原因を絞り込むための順序立てられた手順であり、構文についてはコマンドページへリンクしています。
sidebar:
  order: 30
  label: ワークフロー
i18n:
  sourceHash: 934e7667dbf3
  translator: machine
---

[コマンドリファレンス](../commands/) は各コマンドが何をするかで整理されています。こちらは
何が起きているか（不具合の内容）で整理されています。

| 症状 | ワークフロー |
| --- | --- |
| サイトが一度もオンラインにならない、またはテナントから外れた | [登録](./registration/) |
| 経路が広告されない、または学習されない。VIP に到達できない | [BGP](./bgp-diagnostics/) |
| トラフィックはノードに届くが、その先に到達しない | [データプレーン](./dataplane-diagnostics/) |

各ステップは、そのコマンドを説明しているページへリンクしています。ここでは内容を繰り返さないため、
構文の記載場所は 1 か所だけであり、ノードの実態との食い違いが生じることはありません。

## いずれの場合でも、まず最初に

どのアクセス経路を使っているかを確認してください。それによって可能なことが変わります。

```bash
curl -sS -H "Authorization: APIToken $XCSH_API_TOKEN" \
  "$XCSH_API_URL/api/config/namespaces/system/sites/<site>" | jq -r '.spec.site_state'
```

`ONLINE` であれば [デバッグ API](../access/debug-api/) が応答します。それ以外の場合は、
登録ワークフローが該当し、登録に依存しない経路のいずれかを使うことになります。ノードにまだ
ネットワーク経路がある場合は [サイトコンソール](../access/site-console/)、ない場合は
[シリアルコンソール](../access/serial-console/) です。

サイトが **完全に見当たらない** 場合、「一度も登録されていない」と結論づける前に除外すべき説明が
もう 1 つあります。それは、誤ったテナントに問い合わせているという可能性です。`cd terraform && terraform output -raw
xc_tenant` を実行すると、このデプロイメントが属するテナント名が表示されます。
