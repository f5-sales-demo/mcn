---
title: 裝置上的命令
description: >-
  Site CLI 自身的命令集 — 6 個頂層命令與 82 個 execcli 子命令 — 以及其與 debug API 獨立目錄（34
  個命令）的重疊情況，兩者互不包含。說明執行這些命令的隱藏 execcli 命令、哪些命令有完整文件記錄、哪些僅列出清單，以及命令集的計量方式。
sidebar:
  order: 40
  label: 裝置上
i18n:
  sourceHash: f3baec8d1963
  translator: machine
---

import { Aside, CardGrid } from "@astrojs/starlight/components";
import LinkCard from "@f5-sales-demo/docs-theme/components/LinkCard.astro";

有兩個獨立的命令集可以存取 Customer Edge，且兩者互不包含。

- **裝置上**，Site CLI 擁有 **6 個頂層命令**與 **82 個 `execcli` 子命令**。
- **透過網路**，[debug API](../../access/debug-api/) 有其獨立的 **34 個**命令目錄。

裝置上的大部分介面沒有對應的 API：整個 Vega 控制平面、Envoy 封裝器、封包擷取、kubelet 參數集、`edit-*` 檔案以及輔助 root 存取配對，只能從裝置上存取。

存取這些功能需要 [SSH](../../access/ssh/) 或[序列主控台](../../access/serial-console/)，以及一個在任何文件中都找不到的要點：

<Aside type="tip" title="execcli 是隱藏命令">
`execcli` 不會出現在 Site CLI 的互動選單中。它接受命令名稱作為參數，輸入 `execcli ` 後按 **Tab** 鍵，即可列出全部 82 個命令，並附上裝置本身對每個命令的單行說明。該補全選單是特定版本的權威目錄，也是 `sitecli/exec-catalog.json` 所記錄的內容。
</Aside>

## 計量方式與命令集的重疊情況

以下命令集並非從廠商文件轉錄而來，而是透過 `scripts/sitecli_ssh_harvest.py` 從實際節點讀取，並連同出處一併提交至 `sitecli/exec-catalog.json`。共進行三次運行，分別對應三種註冊狀態：

| 計量時機 | 頂層 | `execcli` |
| --- | --- | --- |
| `site_state: PROVISIONING` | 6 | 82 |
| `site_state: PROVISIONED` | 6 | 82 |
| `site_state: ONLINE` | 6 | 82 |

三次運行均返回**完全相同的命令集** — 名稱相同，不僅僅是總數相同，且最後一次是在不同的部署環境，具有不同的 site 與資源名稱。因此，命令介面不會隨註冊狀態改變，缺少的命令確實不存在，而非尚未提供。

對照 debug API 的 34 個命令，有兩個數字值得釐清：

- **51** 個 `execcli` 子命令沒有對應的 API。
- **55** 是涵蓋整個裝置上介面後的相同計算結果，因為它還包含四個 `configure*` 命令，這些命令位於頂層而非 `execcli` 之下。

兩者均正確；若讀者重新計算其中一個而得到另一個，並非出錯。此處所有數字均來自 `sitecli/catalog.json` 與 `sitecli/exec-catalog.json`。

<Aside type="caution" title="五個不存在的命令">
`factory-reset`、`upgrade`、`reboot`、`show` 與 `clear` **不存在**於此版本中。這些是 Site CLI 可能具備的命令，曾基於此假設被列入本儲存庫的分類中，但任何枚舉都未發現它們。頂層恰好有六個項目：`configure`、`configure-generic-hardware`、`configure-network`、`configure-http-proxy`、`diagnosis` 與 `health`。
</Aside>

<Aside type="caution" title="三個 API 命令不是 execcli 子命令">
在 `execcli` 下尋找這些命令將一無所獲，這看起來像是「命令缺失」：

- **`health`** 與 **`diagnosis`** 是 **頂層** Site CLI 命令。直接在 `>>>` 提示符下輸入，無需加上 `execcli` 前綴。
- **`dig`** 可透過 debug API 存取，但**兩個**裝置上的選單都沒有它，因此它不在下方的直通列表中。它僅限 API 使用。

</Aside>

## 哪些有完整文件，哪些僅列出清單

Customer Edge 運行 Linux 及多個第三方常駐程式。為其命令撰寫文件會與其本身的參考資料重複，且暗示 F5 擁有這些軟體，因此原則是：**為 F5 的軟體撰寫文件，其餘則僅記錄 CE 特有的部分。**

共分三個層級，因為「我們的」與「他們的」並非清晰的界線。此分類記錄於 `sitecli/command-classification.json`。

| 層級 | 處理方式 |
| --- | --- |
| **F5 軟體** | 完整文件 — 用途、語法、參數、擷取的輸出內容、注意事項。Argo、Vega、`vpm`、`vifdump`、`configure*`、`edit-*` 集合、kubelet 參數、root 存取。 |
| **CE 特定解讀** | 其輸出在 CE 上具有特定含義的第三方工具。記錄 CE 特定的解讀方式，工具本身的語義留給上游文件。 |
| **直通** | 純 Linux 或第三方工具，無 CE 特有內容可說明。列為可用，並指向上游文件。無說明文字。 |

此區別決定是否需要建立對應頁面，也是讓本文件不至於淪為 man pages 的劣質副本的關鍵所在。

## F5 軟體

<CardGrid>
  <LinkCard
    title="Vega control plane"
    description="六個 <code>vegactl</code> 命令：設定物件、內部查詢表、追蹤緩衝區，以及哪個節點持有主要角色。"
    href="./vega/"
  />
  <LinkCard
    title="Envoy"
    description="四個 Envoy 管理介面的封裝器 — 監聽器、叢集，以及兩個設定傾印。"
    href="./envoy/"
  />
  <LinkCard
    title="封包擷取"
    description="五個 <code>vifdump</code> 命令。已有文件記錄，但不在此執行：它們會將擷取檔案寫入節點。"
    href="./packet-capture/"
  />
  <LinkCard
    title="Kubelet 參數"
    description="讀取、新增及移除自訂 kubelet 參數。由支援團隊指示操作。"
    href="./kubelet/"
  />
  <LinkCard
    title="vpm 服務"
    description="負責註冊節點並運行平台容器的代理程式。"
    href="./vpm/"
  />
  <LinkCard
    title="設定檔案"
    description="四個 <code>edit-*</code> 命令，用於開啟節點上 F5 擁有的檔案。"
    href="./config-files/"
  />
  <LinkCard
    title="輔助 root 存取"
    description="建立與移除 <code>xuser</code> root 帳號。由支援團隊指示操作，且會留下節點的修改記錄。"
    href="./root-access/"
  />
  <LinkCard
    title="節點設定"
    description="Site CLI 頂層的四個 <code>configure*</code> 命令。"
    href="./configure/"
  />
</CardGrid>

## 直通

可在裝置上使用但**此處不予記錄** — 這些是作業系統與第三方常駐程式，而非 F5 的軟體，其本身的參考資料優於本頁所能提供的任何內容。

| 命令 | 上游文件 |
| --- | --- |
| `ping`, `netstat`, `lsof`, `ip`, `ip-link-show`, `nmcli`, `tracepath` | iproute2, NetworkManager |
| `top`, `check-mem`, `sysctl`, `load-sysctl-conf`, `journalctl`, `files`, `rpm-ostree`, `chronyc-sources` | coreutils, procps, systemd, rpm-ostree, chrony |
| `edit-etc-hosts`, `edit-sysctl-conf` | 這些檔案屬於作業系統 |
| `systemctl-status-crio`, `systemctl-status-docker`, `systemctl-status-kubelet`, `systemctl-status-iscsid`, `systemctl-status-multipathd` | systemd |
| `systemctl-restart-crio`, `systemctl-restart-docker`, `systemctl-restart-kubelet`, `systemctl-restart-iscsid`, `systemctl-restart-multipathd` | systemd — 其中每一個都會中斷節點上的工作負載 |

其中兩個值得注意，即使它們沒有對應頁面。`files` 執行檔案操作，且只會在 `/tmp` 下寫入。而每個 `systemctl-restart-*` 本質上都是破壞性操作 — 重新啟動 `crio` 或 `kubelet` 會中斷該節點上的工作負載。

<Aside type="note" title="七個命令存在於較新版本但不在此版本中">
`collect-database-stats`、`iptables-lv`、三個 `marker-exists-*` 命令、`systemctl-restart-NetworkManager` 與 `systemctl-start-crio-prune` 在此版本中不存在，但存在於版本 `20260703-e2c462a` 中，使該版本擁有 89 個 `execcli` 命令，而此版本為 82 個。[較新版本的命令](../newer-builds/)對此有詳細說明。它們在分類中記錄於 `not_on_this_build.newer_build` 下，以確保此處的內容不會被誤認為可在 `crt-20250613-3382` 上執行的命令。

`collect-debug-info` 是另一個特殊情況：它未出現在任何選單或目錄中。Site CLI 的歡迎文字說明 CLI「允許…為支援收集除錯資訊」，因此該名稱很可能源自該說明文字，而非實際觀察所得。它被標記為未驗證，且刻意不予探測 — 在實際節點上試探性地執行未知命令，正是導致一次 `vifdump` 封包擷取在 CE 仍在註冊時意外啟動的原因。
</Aside>
