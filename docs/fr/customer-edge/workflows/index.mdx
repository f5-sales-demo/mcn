---
title: Workflows de diagnostic
description: >-
  Consultez ce niveau lorsque vous avez un symptôme en tête plutôt qu'une
  commande. Chaque workflow est une séquence ordonnée qui réduit les causes
  possibles, avec des liens vers les pages de commandes pour la syntaxe.
sidebar:
  order: 30
  label: Workflows
i18n:
  sourceHash: 934e7667dbf3
  translator: machine
---

La [référence des commandes](../commands/) est organisée selon ce que fait chaque commande. Ces
pages sont organisées selon ce qui a mal tourné.

| Symptôme | Workflow |
| --- | --- |
| Le site n'est jamais passé en ligne, ou a disparu du tenant | [Enregistrement](./registration/) |
| Les routes ne sont ni annoncées ni apprises ; le VIP est inaccessible | [BGP](./bgp-diagnostics/) |
| Le trafic atteint le nœud mais n'arrive pas à destination | [Plan de données](./dataplane-diagnostics/) |

Chaque étape renvoie à la page qui documente la commande. Rien n'est répété ici,
de sorte que la syntaxe possède un emplacement unique et ne peut pas diverger de celle du nœud.

## Avant chacun d'eux

Déterminez sur quel chemin d'accès vous vous trouvez, car cela change ce qui est possible :

```bash
curl -sS -H "Authorization: APIToken $XCSH_API_TOKEN" \
  "$XCSH_API_URL/api/config/namespaces/system/sites/<site>" | jq -r '.spec.site_state'
```

`ONLINE` signifie que l'[API de débogage](../access/debug-api/) répondra. Toute autre valeur signifie que le
workflow d'enregistrement est celui qu'il vous faut, ainsi que l'un des chemins qui ne dépendent pas de
l'enregistrement : la [Console du site](../access/site-console/) tant que le nœud dispose encore d'un chemin réseau,
la [console série](../access/serial-console/) lorsque ce n'est plus le cas.

Un site **totalement absent** comporte une explication supplémentaire à écarter avant de conclure qu'« il ne s'est
jamais enregistré » : celle d'interroger le mauvais tenant. `cd terraform && terraform output -raw
xc_tenant` indique celui auquel ce déploiement appartient.
