---
title: Diagnose-Workflows
description: >-
  Lesen Sie diese Ebene, wenn Sie ein Symptom statt eines konkreten Befehls im
  Kopf haben. Jeder Workflow ist eine geordnete Abfolge, die die Ursache
  eingrenzt und für die Syntax auf die Befehlsseiten verweist.
sidebar:
  order: 30
  label: Workflows
i18n:
  sourceHash: 934e7667dbf3
  translator: machine
---

Die [Befehlsreferenz](../commands/) ist danach gegliedert, was jeder Befehl tut. Diese
Seiten sind danach gegliedert, was schiefgegangen ist.

| Symptom | Workflow |
| --- | --- |
| Der Standort ist nie online gegangen oder aus dem Tenant verschwunden | [Registrierung](./registration/) |
| Routen werden nicht angekündigt oder gelernt; die VIP ist nicht erreichbar | [BGP](./bgp-diagnostics/) |
| Datenverkehr erreicht den Knoten, kommt aber nicht an | [Datenebene](./dataplane-diagnostics/) |

Jeder Schritt verweist auf die Seite, die den Befehl dokumentiert. Hier wird nichts
wiederholt, damit die Syntax genau einen Ort hat und nicht von der Realität auf dem Knoten
abweichen kann.

## Vor allen anderen Schritten

Klären Sie, auf welchem Zugriffspfad Sie sich befinden, denn er verändert, was möglich ist:

```bash
curl -sS -H "Authorization: APIToken $XCSH_API_TOKEN" \
  "$XCSH_API_URL/api/config/namespaces/system/sites/<site>" | jq -r '.spec.site_state'
```

`ONLINE` bedeutet, dass die [Debug-API](../access/debug-api/) antwortet. Alles andere bedeutet,
dass der Registrierungs-Workflow der richtige ist – zusammen mit einem der Zugriffswege, die
nicht von der Registrierung abhängen: [Standort-Konsole](../access/site-console/), solange der
Knoten noch einen Netzwerkpfad hat, und [serielle Konsole](../access/serial-console/), wenn nicht.

Bei einem Standort, der **vollständig fehlt**, gibt es eine weitere Erklärung auszuschließen,
bevor „er hat sich nie registriert“ gilt: dass Sie den falschen Tenant abfragen. `cd terraform &&
terraform output -raw xc_tenant` nennt den Tenant, zu dem dieses Deployment gehört.
