---
title: Um site que nunca ficou online
description: >-
  Sequência ordenada para um CE que falhou ao registrar, onde a API de depuração
  não pode ajudar porque depende do registro que falhou.
sidebar:
  order: 10
  label: Registro
i18n:
  sourceHash: 0bec51f79119
  translator: machine
---

import { Aside, Steps } from "@astrojs/starlight/components";

Capturado em 28/07/2026 de um CE deste deployment. `sitecli/capture-manifest.json`
registra qual nó, e `scripts/capture-sitecli.sh --check` reverifica a superfície de
comandos contra um CE ativo.

<Aside type="danger" title="A API não pode depurar isto">
A API de depuração alcança o nó através do control plane do F5 Distributed Cloud, pelo
túnel que o registro estabelece. Se o registro falhou não existe túnel,
portanto todos os comandos deste site ficam indisponíveis exatamente para esta falha. Trabalhe a partir do
[Site Console](../../access/site-console/) se o nó ainda tiver um caminho de rede, e do
[console serial](../../access/serial-console/) se não tiver.
</Aside>

<Steps>

1. **Confirme que você está consultando o tenant correto.** Isso custa um comando e vem
   primeiro porque errar nesse ponto faz com que todos os passos seguintes mintam para você: uma frota saudável no tenant
   que você não está olhando é indistinguível de uma frota que nunca se registrou.

   ```bash
   cd terraform
   terraform output -raw xc_tenant       # the tenant this deployment belongs to
   terraform output -raw xc_env_tenant   # the tenant your shell is exporting
   ```

   ```text
   f5-sales-demo
   f5-sales-demo
   ```

   Divergentes, ou uma credencial que você acredita ser válida retornando um `401` puro? Esse é o
   sintoma de um token emitido para outro tenant, e isso já custou a este deployment uma
   reconstrução completa ([issue 696](https://github.com/f5-sales-demo/mcn/issues/696)).

2. **Confirme o que o tenant pensa.** Ausente da lista de sites é um problema
   diferente de presente mas não `ONLINE`.

   ```bash
   curl -sS -H "Authorization: APIToken $XCSH_API_TOKEN" \
     "$XCSH_API_URL/api/config/namespaces/system/sites" | jq -r '.items[].name'
   ```

   Se o site existe e reporta `ONLINE`, o registro foi bem-sucedido e você está no
   workflow errado.

3. **Encontre o registro do site e seu estado.** Um nó pode se registrar e então aguardar
   aprovação, o que parece idêntico a uma falha visto de fora.

   ```bash
   curl -sS -H "Authorization: APIToken $XCSH_API_TOKEN" \
     "$XCSH_API_URL/api/register/namespaces/system/registrations_by_site/<site>" \
     | jq -r '.items[] | "\(.name)  \(.object.status.current_state)  \(.object.spec.gc_spec.infra.hostname)"'
   ```

   ```text
   r-a97e20c8-b7e1-483d-bd76-34a64ff8bc78  ONLINE  f5-xc-ce-vm-01
   ```

   Nenhum item significa que o nó ainda não se registrou — vá para o console serial. Um item
   em um estado diferente de `ONLINE` significa que ele se registrou e algo depois disso falhou, o que
   é uma investigação diferente.

   Para varrer todo o namespace em busca de qualquer coisa aguardando aprovação, `listregistrationsbystate`
   recebe o estado como um **corpo `POST`**:

   ```bash
   curl -sS -X POST -H "Authorization: APIToken $XCSH_API_TOKEN" \
     -H "Content-Type: application/json" \
     -d '{"namespace":"system","state":"PENDING"}' \
     "$XCSH_API_URL/api/register/namespaces/system/listregistrationsbystate" | jq -r '.items[].name'
   ```

   Em uma frota saudável isso não retorna nada, porque a aprovação é automatizada.

4. **Confirme que a VM está realmente em execução**, antes de assumir uma falha de software.

   ```bash
   az vm list -d -g <resource-group> --query "[].{name:name,power:powerState}" -o table
   ```

   ```text
   Name            Power
   --------------  ----------
   ar-ecmp-client  VM running
   f5-xc-ce-vm-01  VM running
   f5-xc-ce-vm-02  VM running
   f5-xc-ce-vm-03  VM running
   ```

5. **Conecte-se ao console serial.** De forma interativa isso requer um terminal real; a própria
   conexão pode ser feita via script sobre o websocket — veja
   [console serial](../../access/serial-console/).

   ```bash
   az serial-console connect -g <resource-group> -n <vm-name>
   ```

   Se recusar, verifique se o diagnóstico de boot está habilitado na VM antes de concluir que o
   nó está inacessível; esse pré-requisito e seus modos de falha estão cobertos na página do
   [console serial](../../access/serial-console/).

6. **Leia o que o console mostra, nesta ordem.** cloud-init primeiro — um nó cujo
   cloud-init nunca foi concluído não tem configuração com a qual se registrar. Depois as
   próprias tentativas de registro, então o DNS: se o nó não consegue resolver
   `register.ves.volterra.io`, nada a jusante pode funcionar.

7. **Uma vez que o nó esteja `ONLINE`**, verifique com [`health`](../../commands/system/health/)
   e espere `state: PROVISIONED`.

</Steps>

<Aside type="caution" title="Um registro é nomeado por um UUID, nunca pelo seu site">
Registros são nomeados `r-<uuid>`, e não existe endpoint que aceite um nome de site onde um
nome de registro é esperado.
`GET .../registrations/<site>` retorna `404`, o que se lê como "este site não tem
registro" e na verdade é "esse não é um nome de registro". O mapeamento site-para-registro
vem apenas de `registrations_by_site/<site>`, como no passo 3, ou listando
`registrations` e casando por `gc_spec.passport.cluster_name`.

A mesma armadilha fez parecer que uma frota saudável tinha zero registros.
</Aside>

## As causas usuais

- **Você está apontado para o tenant errado**, então uma frota que está bem parece ausente. Passo 1.
- O cloud-init não foi concluído, então `/etc/vpm/config.yaml` está ausente ou incorreto.
- O token de registro está expirado, já consumido, ou é de outro tenant.
- Nenhuma rota de saída para `register.ves.volterra.io`.
- Desvio de relógio grande o suficiente para invalidar certificados — rápido de descartar depois com
  [`chronyc-sources`](../../commands/network/chronyc-sources/), mas inacessível
  até que o nó esteja online. O banner de login do [SSH](../../access/ssh/) reporta `NTP: Synced`
  e o status do resolver antes de você executar qualquer coisa, quando o SSH está disponível.
