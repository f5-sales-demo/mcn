---
title: وحدة تحكم الموقع
description: >-
  وحدة تحكم الموقع من F5 لكل عقدة، تُقدَّم عبر منفذ TCP 65500 ويتم الوصول إليها
  من خلال نفق Azure Bastion. لا تحتاج إلى مفتاح SSH ولا مضيف انتقال ولا عنوان IP
  عام — فالوصول قرار Azure RBAC، وهي تستجيب حتى عندما يفشل الموقع في التسجيل.
  تغطي الأوامر الدقيقة ومخرجاتها الفعلية، والشهادة التي ستُحذَّر بشأنها، ولماذا
  يجب أن يستهدف النفق معرّف مورد الجهاز الافتراضي، وكيف ينبغي التعامل مع بيانات
  اعتماد المسؤول.
sidebar:
  order: 22
  label: وحدة تحكم الموقع
i18n:
  sourceHash: d33e2716256e
  translator: machine
---

import { Aside, Steps, CardGrid } from "@astrojs/starlight/components";
import LinkCard from "@f5-sales-demo/docs-theme/components/LinkCard.astro";

تم الالتقاط في 2026-07-28 من إحدى عقد CE في هذا النشر. يسجّل `sitecli/capture-manifest.json`
أي عقدة، ويعيد `scripts/capture-sitecli.sh --check` التحقق من سطح الأوامر
مقابل CE حي.

كل Customer Edge يقدّم وحدة تحكم الموقع من F5 على `https://<sli-ip>:65500`. في هذا
النشر تكون عادةً المسار الأول الذي يُجرَّب.

هي الأقل مطالبةً منك: لا مفتاح SSH لتوزيعه، ولا مضيف انتقال لصيانته، ولا عنوان IP عام على
العقدة، ولا استبدال للجهاز الافتراضي لتمكينها. من يُسمح له بالاتصال يصبح **قرار Azure
RBAC**. وهي لا تحتاج إلى أن يكون الموقع مسجَّلاً: تم التقاط هذه الصفحة أول مرة
بينما كانت المواقع الثلاثة جميعها تُبلّغ عن `site_state: FAILED`، وأُعيد التحقق منها دون تغيير مقابل
العقد نفسها بعد أن أصبحت `ONLINE`.

المشكلة أن وحدة تحكم الموقع تستجيب فقط على عنوان العقدة الداخلي (SLI)،
الذي لا يمكن الوصول إليه إلا من داخل VNet. يسدّ Azure Bastion هذه الفجوة دون وضع
أي شيء آخر على الشبكة.

<Aside type="note" title="ما تم التحقق منه هنا، وما لم يتم">
كل أمر وكل مخرج في هذه الصفحة تم تشغيله مقابل `f5-xc-ce-vm-01` في هذا
النشر وأُعيد إنتاجه حرفيًا، بما في ذلك حالات الفشل.

**تم التحقق من المصادقة**: بيانات اعتماد `admin` الخاصة بالجهاز تُرجع `200` عبر
النفق. ما *لم* يُختبر هو وحدة التحكم نفسها — لا شيء أدناه يصف ما يمكنك
رؤيته أو فعله بمجرد دخولك.

**لم يتم التحقق من Azure RBAC.** جرى كل تشغيل بصفة مالك الاشتراك، لذا فإن متطلب
Reader في الخطوة 2 هو متطلب موثّق من Microsoft وليس شيئًا تم قياسه
هنا.
</Aside>

<Aside type="caution" title="Bastion اختياري بالاشتراك، ولا يُنشر افتراضيًا">
القيمة الافتراضية لـ `enable_bastion` هي `false`، لذا عند تنفيذ `terraform apply` افتراضي لا يوجد مضيف Bastion
ويكون `terraform output -raw bastion_name` هو `null`. اضبط `enable_bastion = true` في
`terraform/terraform.tfvars` — وهي `true` في هذا النشر.

سبب كونه اختياريًا هو التكلفة وليس المخاطرة: مضيف من فئة Standard SKU يفرض رسمًا ثابتًا بالساعة
منذ لحظة توفيره، سواء فتح أحد نفقًا أم لا — 0.29 دولار
أمريكي/الساعة في `eastus` لوحدتي القياس المضمّنتين (سعر التجزئة، 2026-07)، أي ما يقارب 212 دولارًا أمريكيًا
شهريًا. أوقف تشغيله مرة أخرى عندما لا يحتاج العرض التوضيحي إلى الوصول إلى وحدة التحكم.
</Aside>

## قبل أن تبدأ

<Steps>

1. سجّل الدخول إلى Azure وأضف امتداد Bastion. الأنفاق ميزة خاصة بالعميل الأصلي
   والامتداد غير مثبت افتراضيًا.

   ```bash
   az login
   az extension add --name bastion
   ```

2. احصل على دور **Reader** على الجهاز الافتراضي لـ CE، وواجهة الشبكة الخاصة به، ومضيف Bastion.
   هذا متطلب موثّق من Microsoft وهو **غير مُتحقَّق منه هنا** — كل تشغيل في هذا
   النشر جرى بصفة مالك الاشتراك، لذا لم يُثبت أن أي دور أدنى
   كافٍ أو غير كافٍ.

3. تأكد من أن Bastion منشور وقادر. لا تدعم فئة Basic SKU الأنفاق ولا
   الاتصال المستند إلى IP، لذا فإن `Standard` و`True` هنا كلاهما أساسيان.

   ```bash
   az network bastion list -g "$(terraform output -raw resource_group_name)" \
     --query "[].{name:name, sku:sku.name, tunneling:enableTunneling}" -o table
   ```

   ```text
   Name               Sku       Tunneling
   -----------------  --------  -----------
   mcn-ce-ha-bastion  Standard  True
   ```

</Steps>

## افتح النفق

خذ القيم من `terraform output` بدلًا من كتابتها — فمعرّف المورد ليس
شيئًا يُجمَّع يدويًا.

<Steps>

1. اقرأ اسم مضيف Bastion، ومجموعة الموارد، ومعرّف مورد الجهاز الافتراضي لـ CE المستهدف.

   ```bash
   cd terraform
   BASTION=$(terraform output -raw bastion_name)
   RG=$(terraform output -raw resource_group_name)
   VM_ID=$(terraform output -json ce_vm_ids | jq -r '.eastus01')
   ```

   ```text
   mcn-ce-ha-bastion
   rg-mcn-ce-ha-rmordasiewicz
   /subscriptions/…/providers/Microsoft.Compute/virtualMachines/f5-xc-ce-vm-01
   ```

   <Aside type="caution" title="الخريطة مفهرسة حسب الموقع، وليس حسب اسم الجهاز الافتراضي">
   مفاتيح `ce_vm_ids` هي `eastus01` و`eastus02` و`eastus03` — **وليست** أسماء الأجهزة الافتراضية.
   مرشّح مثل `jq -r '."f5-xc-ce-vm-01"'` يُرجع `null`، ثم يفشل النفق بسبب
   معرّف مورد فارغ. اعرضها إن لم تكن متأكدًا:

   ```bash
   terraform output -json ce_vm_ids | jq -r 'keys | join(", ")'
   ```

   ```text
   eastus01, eastus02, eastus03
   ```

   </Aside>

2. ابدأ النفق. يُبقي المنفذ مفتوحًا حتى يتم إيقافه، لذا شغّله في الخلفية أو استخدم
   طرفية ثانية.

   ```bash
   az network bastion tunnel \
     --name "$BASTION" \
     --resource-group "$RG" \
     --target-resource-id "$VM_ID" \
     --resource-port 65500 \
     --port 65500 &
   ```

   ```text
   WARNING: Opening tunnel on port: 65500
   WARNING: Tunnel is ready, connect on port 65500
   WARNING: Ctrl + C to close
   ```

   جميع الأسطر الثلاثة مسبوقة بـ `WARNING:`. هذا هو Azure CLI يكتب التقدّم إلى
   stderr، وليس مشكلة. انتظر ظهور `Tunnel is ready` قبل الاتصال.

3. تأكد من أن المستمع محلي، على loopback:

   ```bash
   lsof -nP -iTCP:65500 -sTCP:LISTEN
   ```

   ```text
   COMMAND   PID  USER  FD  TYPE  DEVICE  SIZE/OFF  NODE  NAME
   Python  41648  you   7u  IPv4  …            0t0   TCP  127.0.0.1:65500 (LISTEN)
   ```

4. افتح وحدة التحكم. النفق يجعلها محلية، لذا فالعنوان هو `localhost` — وليس
   عنوان العقدة.

   ```bash
   open https://localhost:65500/          # macOS
   xdg-open https://localhost:65500/      # Linux
   ```

5. أوقف النفق عند الانتهاء — `Ctrl-C`، أو `kill %1` إن كنت قد شغّلته في الخلفية.
   تركه مفتوحًا يحتفظ بالمنفذ المحلي 65500 وبجلسة Bastion.

</Steps>

## ما الذي ستراه

**مربع حوار لبيانات الاعتماد، وليس صفحة تسجيل دخول.** تستخدم وحدة التحكم مصادقة HTTP Basic، لذا
يعرض المتصفح مطالبته الخاصة باسم المستخدم وكلمة المرور. لا يوجد نموذج للنظر إليه:

```bash
curl -skI https://localhost:65500/
```

```text
HTTP/1.1 401 Unauthorized
WWW-Authenticate: Basic realm="Volterra Site Console"
Content-Type: text/html; charset=utf-8
Content-Length: 0
```

الاستجابة `401` بذلك النطاق تعني أن وحدة التحكم تعمل بشكل صحيح. هذا ما يُفترض أن يحصل عليه الطلب
غير المصادق عليه — وتقديم بيانات اعتماد `admin` إلى نفس عنوان URL يُرجع
`200`، وهي الطريقة التي تؤكد بها بيانات الاعتماد قبل فتح المتصفح:

```bash
curl -sk -u admin -o /dev/null -w '%{http_code}\n' https://localhost:65500/
```

```text
Enter host password for user 'admin':
200
```

استخدام `-u admin` بدون نقطتين يجعل `curl` يطالب بكلمة المرور بدلًا من أخذها من
سطر الأوامر، حيث ستنتهي في سجل الأوامر وفي قائمة العمليات.

**تحذير بشأن الشهادة، لثلاثة أسباب في آن واحد.** بفحص الشهادة مباشرة:

```bash
openssl s_client -connect localhost:65500 -servername localhost </dev/null 2>/dev/null \
  | openssl x509 -noout -subject -issuer -dates
```

```text
subject=C=US, ST=California, L=Santa Clara, O=Volterra, OU=Volterra Edge Services, CN=site-local.volterra.io
issuer=C=US, ST=California, L=Santa Clara, O=Volterra, OU=Volterra Edge Services, CN=site-local.volterra.io
notBefore=Oct 25 04:51:31 2024 GMT
notAfter=Oct 25 04:51:31 2025 GMT
```

الموضوع مطابق للمُصدِر، إذن فهي موقّعة ذاتيًا. الاسم هو `site-local.volterra.io` بينما
أنت تتصل بـ `localhost`، لذا لن يتطابق. وفي الإصدار الذي يعمل عليه هذا النشر،
**انتهت صلاحية الشهادة في 25 أكتوبر 2025** — الجهاز يأتي بها، ولا شيء
في هذا المستودع يجدّدها.

توقّع تحذيرًا صريحًا من المتصفح، وتوقّعه في كل مرة. سطر الموضوع أعلاه هو
الجزء المفيد: به تتأكد أنك وصلت إلى الجهاز وليس إلى شيء آخر
يستمع على المنفذ المحلي 65500.

## بيانات اعتماد المسؤول

سجّل الدخول باسم `admin`. **هذا التوثيق لا ينشر كلمة المرور، ولا ينبغي له.**

<Aside type="danger" title="في هذا النشر لا تزال كلمة المرور تلك هي القيمة الافتراضية من مصنع F5">
هذه حقيقة مقاسة وليست شكًا: بيانات الاعتماد الافتراضية من المصنع تُصادَق عليها
وتُرجع `200` على جميع عقد CE الثلاث اليوم. تنشر F5 هذه القيمة الافتراضية وتشترط تغييرها،
ولم يتم ذلك.

النتيجة العملية أن "امتلاك دور Reader على Bastion" في هذا النشر يعادل
وصولًا كاملًا إلى وحدة تحكم الجهاز، لأي شخص يعرف قيمة موجودة في
وثائق F5 العامة. غيّرها، وتعامل مع تغييرها كجزء من نشر العقدة وليس
كتقوية لاحقة.
</Aside>

سببان يجعلان عدم نشرها أهم هنا مما قد يبدو:

- هذا التوثيق عام. طباعة كلمة مرور وحدة التحكم الحالية للأسطول ستحوّل
  "امتلاك دور Reader على Bastion" إلى وصول كامل للجهاز لأي شخص يقرأ الصفحة.
- كلمة المرور المنشورة في التوثيق تصبح خاطئة في اللحظة التي يدوّرها فيها أحدهم، وخاطئة
  بأكثر الطرق تضليلًا — إذ يستنتج القارئ أن وصوله معطّل بدلًا
  من أن الصفحة قديمة.

بمجرد تدويرها، احصل عليها من حيث يحتفظ فريقك بالأسرار التشغيلية.

<Aside type="caution" title="ضبط كلمة المرور من Terraform غير مُتحقَّق منه">
يبدو أن `admin_user_credentials.admin_password` في كائن الموقع هو الطريقة التعريفية
لضبط ذلك. **لم** يُؤكَّد أنه يصل إلى وحدة تحكم الموقع في هذا الإصدار، لذا
لا تفترض أنه كذلك.

لا تقرأ [صفحة SSH](../ssh/) كدليل في أي من الاتجاهين. فاستنتاجها بأن
`admin_password` "لم يغيّر شيئًا" قيس مقابل مصادقة SSH، التي يتجاوزها `vpm`
تمامًا للمستخدم `admin`. وهذا لا يقول شيئًا عن وحدة التحكم.
</Aside>

## لماذا يجب أن يستهدف النفق معرّف المورد

`--target-resource-id` مطلوب. استهداف عنوان العقدة بدلًا من ذلك مرفوض —
أيًا كان العنوان الذي تستخدمه، لذا لم يُطبع هنا عنوان حقيقي عن قصد:

```bash
az network bastion tunnel --name "$BASTION" -g "$RG" \
  --target-ip-address <any-sli-address> --resource-port 65500 --port 65501
```

```text
ERROR: Custom ports are not allowed. Allowed ports for Tunnel with IP connect is 22, 3389.
```

السبب **ليس** أن الاتصال المستند إلى IP معطّل — ففي هذا Bastion قيمة
`ip_connect_enabled` هي `true`. لا تدعم Microsoft المنافذ المخصصة عبر اتصالات العميل الأصلي
المستندة إلى IP بغض النظر عن ذلك الإعداد
([وثائق Azure](https://learn.microsoft.com/azure/bastion/connect-ip-address))، و65500 هو
منفذ مخصص. فحص تكوين Bastion هنا لا يقود إلى شيء؛ استخدم معرّف
المورد.

## عندما لا يعمل

| العَرَض | السبب | الحل |
| --- | --- | --- |
| `ERROR: Custom ports are not allowed. Allowed ports for Tunnel with IP connect is 22, 3389.` | استُخدم `--target-ip-address` | استخدم `--target-resource-id` |
| `ERROR: Defined port is currently unavailable` | نفق آخر يحتفظ بالفعل بذلك المنفذ المحلي | أوقف النفق الآخر، أو مرّر قيمة `--port` مختلفة؛ ويبقى `--resource-port` عند 65500 |
| `terraform output -raw bastion_name` يساوي `null` أو فارغ | Bastion غير منشور | اضبط `enable_bastion = true` ونفّذ apply |
| النفق يبدأ لكن معرّف المورد فارغ | استخدم مرشّح `jq` اسم الجهاز الافتراضي بدلًا من مفتاح الموقع | استخدم المفاتيح `eastus01`/`02`/`03` |
| `401 Unauthorized`، النطاق `Volterra Site Console` | ليس خطأً — طلب غير مصادق عليه | قدّم بيانات اعتماد `admin` |
| المتصفح يرفض الشهادة | موقّعة ذاتيًا، وعدم تطابق الاسم، ومنتهية الصلاحية منذ أكتوبر 2025 | متوقّع؛ تأكد أن الموضوع هو `CN=site-local.volterra.io` وتابع |

<Aside type="note" title="لم يتم إعادة إنتاج حالات فشل التفويض">
عدم كفاية RBAC هو الطريقة الواضحة الأخرى لفشل هذا، لكن إعادة إنتاجه ستعني
إزالة تعيينات الأدوار من نشر مشترك، لذا لم يُوثَّق هنا بخطأ
حرفي. إذا فشل النفق عند البدء بخطأ تفويض، فتحقق من دور Reader على
الأجهزة الثلاثة جميعًا: الجهاز الافتراضي، وواجهة الشبكة الخاصة به، ومضيف Bastion.
</Aside>

## المسارات الأخرى

<CardGrid>
  <LinkCard
    title="Debug API"
    description="قابل للبرمجة والتكرار، ولا يحتاج إلى وصول إلى VNet. 34 أمرًا، ويجب أن يكون الموقع <code>ONLINE</code>."
    href="../debug-api/"
  />
  <LinkCard
    title="SSH"
    description="واجهة سطر أوامر الموقع الكاملة على الجهاز. تحتاج إلى مفتاح موجود منذ أول إقلاع ومضيف داخل VNet."
    href="../ssh/"
  />
  <LinkCard
    title="وحدة التحكم التسلسلية"
    description="لعقدة لا يوجد لها أي مسار شبكة يعمل على الإطلاق. جلسة واحدة لكل جهاز افتراضي، وحد لصق 2,048 حرفًا."
    href="../serial-console/"
  />
</CardGrid>
