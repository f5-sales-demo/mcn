---
title: health
description: >-
  최초로 실행하기에 가장 적합한 단일 명령입니다. 하나의 구조화된 응답으로 전체 사이트의 등록 상태, OS, 리소스 및 서비스 상태를 확인할
  수 있습니다.
sidebar:
  order: 10
  label: health
i18n:
  sourceHash: 0d60c51c2755
  translator: machine
---

import { Aside } from "@astrojs/starlight/components";

## `health`

<Aside type="caution" title="GLOBAL 범위 — 다른 전송 방식">
`command` 배열이 아닌 `global` 경로에 대한 `GET` 요청입니다. `exec-user`에 `health`를 전송하면
`command not supported`가 반환됩니다.

```bash
curl -sS -H "Authorization: APIToken $XCSH_API_TOKEN" \
  "$XCSH_API_URL/api/operate/namespaces/system/sites/<site>/vpm/debug/global/health"
```

</Aside>

```json file=../../../../_data/sitecli-health.txt
```

여기서부터 시작하세요. 실행 비용이 낮고, 구조화되어 있으며, CE에 대한 첫 번째 질문인
`state`에 답을 제공합니다. `PROVISIONED`는 노드가 등록되고 구성되었음을 의미합니다.

그 외의 경우 — `WAITING_FOR_CONFIG`이거나 응답이 전혀 없는 경우 — 데이터 플레인 디버깅을 중단하고
[등록](../../../workflows/registration/)을 확인해야 함을 나타냅니다.

응답에는 OS 및 커널 버전, 기반 VM의 제품 및 벤더 정보, 소프트웨어 빌드 정보도 포함됩니다.
해당 빌드 문자열은 노드에서 사용 가능한 명령을 결정하므로, 명령이 누락되었다고 가정하기 전에 확인해 두는 것이 좋습니다.

응답이 JSON 형식이므로 다음과 같이 깔끔하게 쿼리할 수 있습니다:

```bash
... /vpm/debug/global/health | jq -r '.state, .os_info.os.name'
```
